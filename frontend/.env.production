# Do NOT set NEXT_PUBLIC_API_BASE_URL here.
# Leaving it unset makes config.ts fall back to '/api' (relative path),
# which Nginx proxies to the backend. This avoids mixed-content and
# hardcoded-URL issues in production.

# OnlyOffice via Nginx reverse proxy (relative path)
NEXT_PUBLIC_ONLYOFFICE_URL=/onlyoffice
